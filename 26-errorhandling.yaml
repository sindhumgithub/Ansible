- name: Redirecting the output using the shell module
  hosts: local
  connection: local

  tasks:
  - name: redirect the output
    ansible.builtin.shell: ls -lthr > /tmp/output.txt
    register: redirect_result
    ignore_errors: true

  - name: print the output
    ansible.builtin.debug:
      msg: "{{ redirect_result }}"