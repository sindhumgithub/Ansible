- name: Install packages
  hosts: local
  connection: local

  tasks:
  - name: Print names
    ansible.builtin.package:
      name: "package is:: {{ item }}"
      state: present 
    
    loop:
    - nginx
    - zip

