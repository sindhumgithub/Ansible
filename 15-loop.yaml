- name: Install packages
  hosts: local
  connection: local

  tasks:
  - name: Print names
    ansible.builtin.package:
      name: "{{ item }}"
      state: latest
    
    loop:
    - nginx
    - zip

