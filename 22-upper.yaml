- name: Convert to Upper characters
  hosts: local
  connection: local
  vars:
    name: "Sindhuja Muddana"
    email: Info@GMAIL.COM

  tasks:
  - name: Convert to upper characters
    ansible.builtin.debug:
      msg: "{{ name | upper }}"

  - name: Convert to upper characters
    ansible.builtin.debug:
      msg: "{{ email | lower }}"